[build]
  command = "npm run build"
  publish = "dist"

# SPA routing uchun
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables (Netlify UI orqali sozlang)
# VITE_SUPABASE_URL
# VITE_SUPABASE_ANON_KEY
# GEMINI_API_KEY
